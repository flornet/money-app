<h1 class="uk-h1">Factures périodiques</h1>

<table>
  <thead>
    <tr>
      <th>Active</th>
      <th>User</th>
      <th>Account</th>
      <th>Category</th>
      <th>Checked</th>
      <th>Comment</th>
      <th>Value</th>
      <th>Frequency</th>
      <th>Start date</th>
      <th>End date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @recurrent_entries.each do |recurrent_entry| %>
      <tr>
        <td><%= recurrent_entry.active %></td>
        <td><%= recurrent_entry.user_id %></td>
        <td><%= recurrent_entry.account_id %></td>
        <td><%= recurrent_entry.category_id %></td>
        <td><%= recurrent_entry.checked %></td>
        <td><%= recurrent_entry.comment %></td>
        <td><%= recurrent_entry.value %></td>
        <td><%= recurrent_entry.frequency %></td>
        <td><%= recurrent_entry.start_date %></td>
        <td><%= recurrent_entry.end_date %></td>
        <td><%= link_to 'Show', recurrent_entry %></td>
        <td><%= link_to 'Edit', edit_recurrent_entry_path(recurrent_entry) %></td>
        <td><%= link_to 'Destroy', recurrent_entry, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Recurrent Entry', new_recurrent_entry_path %>
